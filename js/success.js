
document.addEventListener('DOMContentLoaded', () => {
    const App = {
        init() {
            functionDarkMode();
            functionLanguageSelector(translations);
        },
        };
    App.init();
});


